Take rank 99, an unranked row: `main` has no green CI run, and every PR inherits the same red suite. Two `plan-vs-scene` failures are described in `BACKLOG.md`'s "Where things stand" and are nobody's row and have no `SPECS.md` section yet. Container work, no gate.

Start by reproducing both from a clean tree in its own `git worktree` (with a junction for `node_modules`): run `npm test plan-vs-scene` and record which assertions fail and the exact messages, before changing anything. If either does not reproduce, say so and record what you ran, because the two reports come from different days and different machines. Read `HISTORY.md` under rank 1's 2026-09-18 section and #714 and #715 for the earlier write-ups.

**Failure 1: the chapel candles.** At `f5d0c0e`, `plan-vs-scene` fails with `none of the 12 cells between 0.9 and 2.8 m of the chapel candles offers them (the nearest offered "Press E to ring the bell")`. A nearer interaction target out-ranks the one the beat is aimed at. `npm run play` swaps the body and the pouch in the chapel the same way (#714's neighbour).

The open question is a `src/` one nobody has asked: should `InteractionSystem` prefer what the camera points at over what is nearest? That is a behaviour change and it is class O (`architect`). Write the decision and a `SPECS.md` section first, with a recommendation, then build it as class S. The alternative to weigh is fixing the beat rather than the game: aim from a cell where the candles are the nearest target. Pick with evidence: measure what the nearest-versus-aimed-at rule would change for every interactable in the castle, since a rule change that fixes one chapel and breaks a doorway is worse than a moved test position. Record the numbers in `HISTORY.md`.

**Failure 2: the populace beat's baker stop.** `plan-vs-scene.mjs`'s populace beat fails on the baker's first Prime stop, 0.408 m off on the merge of PR #47 (2026-09-18, run 35305787194) and 0.055 m off on rank 1's branch that morning. Two different distances from one stop means a body already walking its ring when the page is read. The beat measures it against `TOL`, which is 0.01 m because that is what the suite diffs static geometry at.

A moving body is owed a different number. Find the ring's step size in `src/populace.js` and the frame time the beat reads at, derive the tolerance from them rather than picking a round one, and hold it as a named constant with a comment saying what it is derived from. Better still, if the beat can put the body at rest first (the way `settle()` collapses smoothing for the player rig), assert the exact position at 0.01 m and keep the strict number. Choose one and say why. Lane C is `data/npcs.json`'s `cast` block and `npc.js`'s body machinery, so keep out of both unless the fix needs them.

**Rules.** #529: nothing here moves an assertion across the `layout` / `plan-vs-scene` / `mystery` / `budget` line. If the fix wants to, stop and classify it as O. #34: for each fix, put the bug back and watch the suite fail, and say which assertion and what it said. #53: a real-time movement or physics failure under software-rendered Chromium is inconclusive, so if a beat behaves differently in CI and on a GPU, say which one you ran and do not treat either as final. #13: no skip list and no known-failures file, so a failing beat is fixed or its cause is written down, and it is never silenced.

**Done** means `npm run build` and `npm test` both pass in a clean tree, and the CI run on the PR is green, which is the first green run on `main` in some time. Update `BACKLOG.md`'s "CI on `main` is red" paragraphs to say what was found and what shipped, and take HISTORY numbers from `origin/main` at the end. Report in at most fifteen lines: files touched, the assertions changed and which one failed when the bug was put back, the `npm test` result, and the numbers claimed.
